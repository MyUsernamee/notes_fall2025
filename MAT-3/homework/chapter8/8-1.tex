\documentclass{article}
\usepackage{custom}
\usepackage{shortex}
\title{ 8-1 }

\begin{document}

\maketitle
\pagebreak

In Exercises 1-2, suppose that $T$ is a mapping whose domain is the vector space $M_{22}$ . In each part, determine whether $T$ is a linear transformation, and if so, find its kernel.
\subsubsection*{ 1 a) }

$T(A) = A^2$
\vfill


\subsubsection*{ b) }

$T(A) = \operatorname{tr}(A)$
\vfill

\pagebreak
In Exercises 3-9, determine whether the mapping $T$ is a linear transformation, and if so, find its kernel.
\subsubsection*{ 5 }

$T:M_{22}\to M_{23}$ , where $B$ is a fixed $2\times 3$ matrix and $T(A) = AB$
\vfill


\subsubsection*{ 6 a) }

$T:M_{22}\to R$ , where $T\left(\left[ \begin{array}{ll}a & b\\ c & d \end{array} \right]\right) = 3a - 4b + c - d$
\vfill

\pagebreak

\subsubsection*{ 7 a) }

$T: P_2 \to P_2$ , where $T(a_{0} + a_{1}x + a_{2}x^{2}) = a_{0} + a_{1}(x + 1) + a_{2}(x + 1)^{2}$
\vfill


\subsubsection*{ 19 }

Consider the basis $S = \{\mathbf{v}_1, \mathbf{v}_2\}$ for $R^2$ , where $\mathbf{v}_1 = (1,1)$ and $\mathbf{v}_2 = (1,0)$ , and let $T: R^2 \to R^2$ be the linear operator for which

$$
T (\mathbf {v} _ {1}) = (1, - 2) \text {a n d} T (\mathbf {v} _ {2}) = (- 4, 1)
$$

Find a formula for $T(x_{1},x_{2})$ , and use that formula to find $T(5, - 3)$
\vfill

\pagebreak

\subsubsection*{ 21 }

Consider the basis $S = \{\mathbf{v}_1,\mathbf{v}_2,\mathbf{v}_3\}$ for $R^3$ , where

$\mathbf{v}_1 = (1,1,1),\mathbf{v}_2 = (1,1,0),$ and $\mathbf{v}_3 = (1,0,0)$ and let $T:R^3\to R^3$ be the linear operator for which

$$
\begin{array}{l} T \left(\mathbf {v} _ {1}\right) = (2, - 1, 4), \quad T \left(\mathbf {v} _ {2}\right) = (3, 0, 1), \\ T \left(\mathbf {v} _ {3}\right) = (- 1, 5, 1) \\ \end{array}
$$

Find a formula for $T(x_{1},x_{2},x_{3})$ , and use that formula to find $T(2,4, - 1)$

\pagebreak
\vfill


\subsubsection*{ Answers }

1. (a) Nonlinear

5. Linear; kernel consists of all $2\times 2$ matrices whose rows are orthogonal to all columns of $B$

7. (a) Linear; $\ker (T) = \{0\}$

17. (a) $(1,0,1)$

19. $T(x_{1},x_{2}) = (-4x_{1} + 5x_{2},x_{1} - 3x_{2})$ . $T(5, - 3) = (-35,14)$

21. $T(x_{1},x_{2},x_{3}) = (-x_{1} + 4x_{2} - x_{3},5x_{1} - 5x_{2} - x_{3},x_{1} + 3x_{3});T(2,4, - 1) = (15, - 9, - 1)$
\vfill

\pagebreak

\end{document}